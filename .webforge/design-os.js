(function(){const cfg=structuredClone(window.SHAOLIN_SITE||{});const root=document.getElementById('token-controls');const frame=document.getElementById('preview');Object.entries(cfg.tokens||{}).forEach(([k,v])=>{const row=document.createElement('label');row.className='control';row.innerHTML=`${k}<input type="color" value="${v}" data-token="${k}">`;root.appendChild(row)});function doc(){try{return frame.contentDocument}catch(e){return null}}root.addEventListener('input',e=>{if(!e.target.dataset.token)return;cfg.tokens[e.target.dataset.token]=e.target.value;const d=doc();if(d)d.documentElement.style.setProperty('--'+e.target.dataset.token,e.target.value)});document.querySelectorAll('[data-size]').forEach(b=>b.onclick=()=>{frame.className='frame '+(b.dataset.size==='desktop'?'':b.dataset.size)});frame.addEventListener('load',()=>{document.getElementById('seoTitle').value=cfg.seo.title;document.getElementById('seoDesc').value=cfg.seo.description});['hero1','hero2','hero3','hero4'].forEach((id,i)=>{document.getElementById(id).addEventListener('input',e=>{const d=doc();const el=d&&d.querySelectorAll('.hero h1 > span')[i];if(el)el.textContent=e.target.value})});document.getElementById('seoTitle').addEventListener('input',e=>cfg.seo.title=e.target.value);document.getElementById('seoDesc').addEventListener('input',e=>cfg.seo.description=e.target.value);document.getElementById('export').onclick=()=>{const d=doc();cfg.hero={line1:d?.querySelectorAll('.hero h1 > span')[0]?.textContent||'',line2:d?.querySelectorAll('.hero h1 > span')[1]?.textContent||'',line3:d?.querySelectorAll('.hero h1 > span')[2]?.textContent||'',line4:d?.querySelectorAll('.hero h1 > span')[3]?.textContent||''};const blob=new Blob([JSON.stringify(cfg,null,2)],{type:'application/json'});const a=document.createElement('a');a.href=URL.createObjectURL(blob);a.download='shaolin-slurp-v3-approved-config.json';a.click();URL.revokeObjectURL(a.href)}})();